/*

package com.example.healingfoodlab.config;

import com.example.healingfoodlab.config.DiagnosisResultInterceptor;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new DiagnosisResultInterceptor())
                .addPathPatterns("/usr/diagnosis/DiagnosisResult"); // ✅ 보호할 URL 패턴 지정
    }
}

*/
